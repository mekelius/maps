#ifndef __NUMBER_TYPES_USED
#define __NUMBER_TYPES_USED

/**
 * This file includes global definitions for th enumeric types used internally.
 * Changing these will probably break something, but we need to document where they
 * are used as future-proofing in case we want to change them at some point.
 */

namespace Maps {

using int_t = long;
using float_t = double;

} // namespace Maps

#endif